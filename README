Retention Filter (Linux/MacOS)
This Script runs through a Directory and lists which sub-directories are older than the User Defined amount of days!

run this command through terminal [replace anything in brackets with what you want]

    python3 main.py [filepath of where you want searched] [retention days (by default its 90)] > [outputfilename].txt

        SAMPLE: python3 main.py /Volumes/Drive 90 > retention.txt
